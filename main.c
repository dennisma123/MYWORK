#include<stdio.h>
void main()
{
  int value;
  scanf("value is %d", &value);
  printf("changed in worksapce, and staged but not commit\n");
  printf("readme changed in remote repo, and changed main.c to commit 
           test if ok to push\n");
  printf("changg main.c in remote repo, test if changed in main.c in local repo\n");
  i will add a printf here!!!
  return;
}
